# Telegram bot token (scoped to your bot only)
TELEGRAM_BOT_TOKEN=replace_with_short_lived_token

# GitHub token for automation (fine-grained, repo-scoped, PR-only if possible)
GITHUB_TOKEN=replace_with_short_lived_repo_scoped_token
